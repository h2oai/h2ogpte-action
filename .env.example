# dotenv-linter:off IncorrectDelimiter

# Do not commit your actual .env file to Git! This may contain secrets or other
# private information.

# Enable/disable step debug logging (default: `false`). For local debugging, it
# may be useful to set it to `true`.
ACTIONS_STEP_DEBUG=true

# GitHub Actions inputs should follow `INPUT_<name>` format (case-sensitive).
# Hyphens should not be converted to underscores!
INPUT_MILLISECONDS=2400

# =============================================================================
# h2oGPTe Action Environment Variables for Local Testing
# =============================================================================
# Copy this file to .env and fill in the required values to test the action locally.
# This mocks the GitHub Actions environment for development and testing.

# Required: GitHub access token for API calls
# Get a personal access token from: https://github.com/settings/tokens
# Required scopes: repo, workflow (for write permissions)
GITHUB_TOKEN=your_github_token_here

# Required: h2oGPTe API key
# Get your API key from: https://h2ogpte.genai.h2o.ai/api
H2OGPTE_API_KEY=your_h2ogpte_api_key_here

# Required: Mock GitHub Actions run ID (any numeric string)
GITHUB_RUN_ID=123456789

# Required: Mock repository in owner/repo format
GITHUB_REPOSITORY=your-username/your-repo

# Optional: GitHub API base URL (default: https://api.github.com)
GITHUB_API_URL=https://api.github.com

# Optional: GitHub server base URL (default: https://github.com)
GITHUB_SERVER_URL=https://github.com

# Optional: h2oGPTe API base URL (default: https://h2ogpte.genai.h2o.ai)
H2OGPTE_API_BASE=https://h2ogpte.genai.h2o.ai

# Optional: Language model to use (default: "auto")
# Options: "auto", or specific model names
LLM=auto

# Optional: Maximum number of turns the agent can take (default: "auto")
# Options: "auto", 5, 10, 15, 20
AGENT_MAX_TURNS=auto

# Optional: Agent accuracy setting (default: "standard")
# Options: "quick", "basic", "standard", "maximum"
AGENT_ACCURACY=standard

# Optional: Total timeout in seconds for all agent processing (default: 3600)
AGENT_TOTAL_TIMEOUT=3600

# Optional: Custom prompt for workflows
# String injection options: {{repoName}}, {{idNumber}}, {{eventsText}}
# PROMPT=your_custom_prompt_here
